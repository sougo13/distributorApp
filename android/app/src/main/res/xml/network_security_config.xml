<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Разрешение для конкретного домена (рекомендуется для самоподписанных) -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">buyers.distributor.name</domain>
        <trust-anchors>
            <!-- Доверяем системным сертификатам -->
            <certificates src="system" />
            <!-- Доверяем пользовательским сертификатам (полезно, если сертификат установлен на устройстве/эмуляторе) -->
            <certificates src="user" />
        </trust-anchors>
    </domain-config>

    <!-- 
        В качестве альтернативы для разработки можно разрешить все пользовательские сертификаты 
        для всех доменов в отладочных сборках. Не используйте в продакшене.
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
    -->

    <!-- Базовая конфигурация для остальных доменов -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config> 